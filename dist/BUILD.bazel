load("//:diff.bzl", "diff", "update_patches")

package(default_visibility = ["//visibility:private"])

diff(
    name = "module_bazel",
    source = "//:MODULE.bazel",
    target = ":MODULE.bazel",
)

diff(
    name = "build_bazel",
    source = "//:BUILD.bazel",
    target = ":BUILD.root.bazel",
)

diff(
    name = "git_build_bazel",
    source = "//git:BUILD.bazel",
    target = ":BUILD.git.bazel",
)

update_patches()
