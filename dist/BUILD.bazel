load("//rules:patched_file.bzl", "patched_file_with_diff_test")

package(default_visibility = ["//visibility:private"])

patched_file_with_diff_test(
    name = "lib_build_bazel",
    src = "//lib:BUILD.bazel",
    file = ":lib.BUILD.bazel",
    patch = ":lib_build_bazel.patch",
)

patched_file_with_diff_test(
    name = "module_bazel",
    src = "//:MODULE.bazel",
    file = ":MODULE.bazel",
    patch = ":module_bazel.patch",
)
