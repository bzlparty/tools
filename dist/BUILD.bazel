load("//:diff.bzl", "diff", "update_patches")

package(default_visibility = ["//visibility:private"])

diff(
    name = "module_bazel",
    source = "//:MODULE.bazel",
    target = ":MODULE.bazel",
)

diff(
    name = "root_build_bazel",
    source = "//:BUILD.bazel",
    target = ":root.BUILD.bazel",
)

diff(
    name = "git_build_bazel",
    source = "//git:BUILD.bazel",
    target = ":git.BUILD.bazel",
)

update_patches()
